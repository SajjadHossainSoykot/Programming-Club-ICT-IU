#include <stdio.h>
void main()
{
   float l, w, a;
    printf("Enter the Length of Rectangular:");
    scanf("%f", &l);
    printf("Enter the Width of Rectangular:");
    scanf("%f", &w);
    a=l*w;
    /*vala asoni*/
    printf("The Area of the Rectangular is %0.0f ", a);
    return 0;
}
